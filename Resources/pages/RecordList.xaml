<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="ECHO_PRINT.Resources.pages.RecordList"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="Recording List">

    <!-- background -->
    <ContentPage.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#0B1220" Offset="0" />
            <GradientStop Color="#111827" Offset="1" />
        </LinearGradientBrush>
    </ContentPage.Background>

    <RefreshView x:Name="refresh" Refreshing="OnRefresh">
        <CollectionView x:Name="audioList" SelectionMode="None" ItemsLayout="VerticalList"  Margin="16,8">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Padding="14" Margin="0,10" CornerRadius="16" HasShadow="True" BackgroundColor="Snow">
                        <Frame.Shadow>
                            <Shadow Brush="#55000000" Radius="16" Opacity="0.6" Offset="0,8"/>
                        </Frame.Shadow>

                        <VerticalStackLayout Spacing="12">

                            <!-- preview photo-->
                            <Border HeightRequest="160"  StrokeThickness="0"  BackgroundColor="#1F2A37" StrokeShape="RoundRectangle 12">
                                
                                <Image Source="{Binding PhotoPath}" Aspect="AspectFill" HeightRequest="160">
                                    
                                    <Image.GestureRecognizers>
                                        <TapGestureRecognizer Tapped="OnPhotoTapped"
                                                              CommandParameter="{Binding}" />
                                    </Image.GestureRecognizers>
                                    
                                </Image>

                                <Border.Triggers>
                                    <!--hide when no photo -->
                                    <DataTrigger TargetType="Border" Binding="{Binding PhotoPath}" Value="{x:Null}">
                                        <Setter Property="IsVisible" Value="False"/>
                                    </DataTrigger>
                                    <DataTrigger TargetType="Border" Binding="{Binding PhotoPath}" Value="">
                                        <Setter Property="IsVisible" Value="False"/>
                                    </DataTrigger>
                                </Border.Triggers>
                            </Border>

                            <!-- photo picker -->
                            <Button Text="Attach Photo" CommandParameter="{Binding}" Clicked="MediaPcker" FontSize="12"  TextColor="White" BackgroundColor="#3A7CA5"  CornerRadius="12"  Padding="12,8" />


                            <Grid ColumnDefinitions="*,Auto,Auto,Auto"  ColumnSpacing="12">
                                
                                

                                <!-- title -->
                                <Label Grid.Column="0" Text="{Binding Title}" TextColor="#0e2f44" FontAttributes="Bold"  FontSize="16" LineBreakMode="TailTruncation" />
                                <!-- Fav button -->
                                <Button  Text="♥" CommandParameter="{Binding}" FontSize="20"   Clicked="OnFav"  BackgroundColor="#00000000" Padding="1,1"  WidthRequest="44" HorizontalOptions="End"  >
                                    <Button.Triggers>
                                        <!-- when fav is true-->
                                        <DataTrigger TargetType="Button" Binding="{Binding IsFav}" Value="True">
                                            <Setter Property="TextColor" Value="#F25F5C"/>
                                            <Setter Property="BorderColor" Value="#F25F5C"/>
                                        </DataTrigger>
                                        <!-- when fav is false -->
                                        <DataTrigger TargetType="Button" Binding="{Binding IsFav}" Value="False">
                                            <Setter Property="TextColor" Value="#9CA3AF"/>
                                            <Setter Property="BorderColor" Value="#334155"/>
                                        </DataTrigger>
                                    </Button.Triggers>
                                    <Button.BorderWidth>0</Button.BorderWidth>
                                    <Button.CornerRadius>12</Button.CornerRadius>
                                </Button>


                                <!-- play -->
                                <Button x:Name="PlayBtn" Grid.Column="1" Text="Play" CommandParameter="{Binding}" Clicked="OnPlay" BackgroundColor="#00000000" TextColor="#3A7CA5" BorderColor="#334155" BorderWidth="1" CornerRadius="12"
                                        Padding="12,8" />

                                <!-- map -->
                                <Button Grid.Column="2" Text="📍" CommandParameter="{Binding}" Clicked="OpenMap" BackgroundColor="#00000000"  TextColor="#3A7CA5" BorderColor="#334155" BorderWidth="1" CornerRadius="12" Padding="10,6"
                                        WidthRequest="44" />

                                <!-- delete -->
                                <Button Grid.Column="3" Text="Delete" CommandParameter="{Binding}" Clicked="OnDelete" TextColor="White" BackgroundColor="#F25F5C" CornerRadius="12" Padding="12,8" />
                            </Grid>


                            <!-- meta data -->
                            <HorizontalStackLayout Spacing="14">
                                <Label Text="{Binding CreatedOn, StringFormat='{}{0:dd MMM yyyy HH:mm}'}" FontSize="12" TextColor="#3A7CA5" />
                                <Label Text="{Binding PlaceName}" FontSize="12"
                                       TextColor="#3A7CA5" />
                            </HorizontalStackLayout>

                            <!-- progressbar -->
                            <ProgressBar x:Name="progressBar" HeightRequest="4" Progress="0" IsVisible="False" Margin="0,4,0,0" />

                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </RefreshView>
</ContentPage>
